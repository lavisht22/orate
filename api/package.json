{
  "name": "orate-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply orate-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply orate-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250405.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.0.0"
  }
}
